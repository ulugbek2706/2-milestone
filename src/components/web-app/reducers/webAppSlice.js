import { createSlice } from "@reduxjs/toolkit";


const initialState = {

};

const WebAppSlice=createSlice({
    name:"webapp",
    initialState,
    reducers:{

    }
});

export const {}=WebAppSlice.actions;
export default WebAppSlice.reducer;
